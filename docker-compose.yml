services:
  minio:
    image: minio/minio:latest
    container_name: minio-storage
    ports:
      - "9000:9000"
      - "9001:9001"
    env_file:
      - ./configs/s3-storage.env
    entrypoint: >
       /bin/sh -c "
       /usr/bin/mc mb data/medical-certificates-bucket;
       minio server /data --console-address ":9001";
       "
    volumes:
      - ./databases-data/minio-data:/data
    networks:
      - s3-network
networks:
  s3-network:
    driver: bridge
